# Keycloak (NAS)
KEYCLOAK_ISSUER_URI=http://<NAS_IP>:8180/realms/vanilla2hub

# Database (docker compose 기본값과 동일하면 생략 가능)
DB_ROOT_PASSWORD=root
DB_NAME=vanilla2hub
DB_USERNAME=vanilla2hub
DB_PASSWORD=vanilla2hub
